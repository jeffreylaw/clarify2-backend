<!DOCTYPE html>
<html lang="en">
<head>

</head>
<body>
    <div>
        <a href='/'>Home</a>
        <a href='/register'>Register</a>

        <% if (typeof token != 'undefined' && token) { %>
            <a href='/logout'>Logout</a>
        <% } else {%>
            <a href='/login'>Login</a>
        <% } %>
    </div>
    <%- body %>
</body>
</html>